version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "${PORT:-3500}:3500"
    environment:
      - PORT=3500
      - DATABASE_URL=${DATABASE_URL}
      - NODE_ENV=production
